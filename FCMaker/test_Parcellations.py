import Parcellations


##TODO fill out tests using pytests and assert
#This will take the .net files and place the values of the correlations at the
# correct location according to the new ordering
# def test_build_Matrix(parcellation, net_file, fisherz=False, unordered=False):
#     assert
#     return
#
#
# def test_write_new_order(new_order,outfile):
#     return
#
# #Plot the matrix to a corr matrix
# def test_plot_correlation_matrix(parcellation, subject, output_file, matrix, max, min, fisherz=False):
#     return
#
# #write the matrix to a csv
# def test_write_out_correlation(matrix, out_file):
#     return
#
# #Check to see if we have setup that parcellation scheme
# def test_check_input(net_file):
#     return
#
# #Encapsulates all the processing
# def test_run(net_file, parcellation, outputDir, fisherz, scaleBounds, outputUnordered, outputNewOrder):
#     return